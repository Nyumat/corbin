/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cloudflare-ipfs.com",
      "loremflickr.com",
      "avatars.githubusercontent.com",
    ],
  },
};

export default nextConfig;
